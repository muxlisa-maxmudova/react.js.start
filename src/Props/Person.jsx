const Person = (props) => {
    return (
        <div>
            <h1>
                Person
            </h1>
            <p>Name: {props.name}</p>
            <p>Name: {props.age}</p>
        </div>
    )
}
export default Person
